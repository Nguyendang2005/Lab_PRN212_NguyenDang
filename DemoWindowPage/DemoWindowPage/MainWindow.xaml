<Window x:Class="DemoWindowPage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoWindowPage"
        mc:Ignorable="d"
        Title="WPF Demo" Height="520" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1F1F1F" Padding="12,8">
            <TextBlock x:Name="CurrentPageTitle"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Text="WPF Demo" />
        </Border>

        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden" />

        <Border Grid.Row="2" Background="WhiteSmoke" BorderThickness="1,1,1,0" BorderBrush="#D0D0D0" Padding="8">
            <WrapPanel HorizontalAlignment="Center">
                <Button Content="Page 1" Click="ToPage1_Click" Width="90" Height="30" Margin="4" />
                <Button Content="Page 2" Click="ToPage2_Click" Width="90" Height="30" Margin="4" />
                <Button Content="Grid Panel" Click="ToPage3_Click" Width="90" Height="30" Margin="4" />
                <Button Content="Dock Panel" Click="ToPage4_Click" Width="90" Height="30" Margin="4" />
                <Button Content="Wrap Panel" Click="ToPage5_Click" Width="90" Height="30" Margin="4" />
                <Button Content="Stack Panel" Click="ToPage7_Click" Width="90" Height="30" Margin="4" />
            </WrapPanel>
        </Border>
    </Grid>
</Window>
